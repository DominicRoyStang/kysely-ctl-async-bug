## kysely-ctl-async-bug

## Steps

1. Run `npm install`
3. Run `npx kysely migrate:make test` // This fails with SyntaxError: await is only valid in async functions and the top level bodies of modules
